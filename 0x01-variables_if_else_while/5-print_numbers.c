#include <stdio.h>
#include <stdlib.h>
/**
 * main - program that puts a single digit number starting with base 10
 *
 * Return: Always 0 (success)
 */
int main(void)
{
	int a;

	for (a = 0; a <= 10; a++)
		putchar (a);
	putchar ('\n');
	return (0);
}

